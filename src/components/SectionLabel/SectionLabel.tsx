const SectionLabel = ({ children }: { children: React.ReactNode }) => (
    <div className="font-sans text-base font-bold tracking-widest uppercase text-accent mb-4">
        {children}
    </div>
);

export default SectionLabel;
